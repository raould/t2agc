(program
  (import { Priority TaskStatus } "./types")

  (export (type ExecuteFn (tfn () any)))

  (export (interface Task
    (Task
      (execute ExecuteFn)
      (id number)
      (priority Priority)
      (status TaskStatus)
    )
  ))
)